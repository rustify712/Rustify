请你按照以下格式返回代码块，表示变更代码块：

```lang:filepath:start_line:end_line
code
```

其中：
- `lang` 是代码块的编程语言。
- `filepath` 是变更代码所在文件的路径。
- `start_line` 是变更的起始行号（从 1 开始）。
- `end_line` 是变更的结束行号（从 1 开始）。
- `code` 是具体的代码片段。
- `start_line` 一定小于或等于 `end_line`，且 `start_line` 和 `end_line` 之间的行都是变更的内容。

**值得注意的是：**
- 变更代码块可以表示创建、增加、删除、修改等操作。
- 在使用变更代码块表示修改或删除操作时，需要确保变更代码块涵盖了所有要变更的内容，包括但不限于代码、注释等。
    - 例如：删除某个结构体定义时，需要确保结构体定义以及相关的注释、注解等内容都包含在变更代码块中。
- 在使用变更代码块表示增加操作时，需要确保插入位置是安全的，不会破坏原有的代码结构。
- 变更代码块可以跨越多行，但是不可以跨越多个文件。
- 变更代码块可以有多个，但是不可以有重叠。
- 多个变更代码块应该是线性的，即按照从上到下的顺序依次变更。

### 创建操作
例如：创建文件 main.rs，应采用的变更代码块为：
```rust:main.rs:1:1
```

### 增加操作
例如：对于以下代码片段：
1:fn main() {
2:}
在第 2 行插入一行代码 println!("Hello, world!");，应采用的变更代码块为：
```rust:main.rs:2:2
    println!("Hello, world!");
```
值得注意的是，在 x 行插入内容时，`start_line` 和 `end_line` 都是 x。

### 删除操作
例如：对于以下代码片段：
1:fn main() {
2:    println!("Hello, world!");
3:}
4:
5:// This is a comment for the main function.
6:fn main() {
7:    println!("Hello, world!");
8:}
删除多余的 main 函数应采用的变更代码块为：
```rust:main.rs:5:8
```
值得注意的是，在这个例子中，删除第二个 main 函数的同时也删除了与其相关的注释，因此变更代码块的开始行号是 5，结束行号是 8。

### 修改操作
例如：对于以下代码片段：
1:fn main() {
2:    println!("Hello, world!");
3:}
将 main 函数修改为 hello 函数，应采用的变更代码块为：
```rust:main.rs:1:1
fn hello() {
```